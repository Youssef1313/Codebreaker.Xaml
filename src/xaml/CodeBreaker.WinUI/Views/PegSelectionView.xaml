<UserControl
    x:Class="CodeBreaker.WinUI.Views.PegSelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeBreaker.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
            <ComboBox ItemsSource="{x:Bind ViewModel.ColorList, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedColor1, Mode=TwoWay}"></ComboBox>
            <ComboBox ItemsSource="{x:Bind ViewModel.ColorList, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedColor2, Mode=TwoWay}"></ComboBox>
            <ComboBox ItemsSource="{x:Bind ViewModel.ColorList, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedColor3, Mode=TwoWay}"></ComboBox>
            <ComboBox ItemsSource="{x:Bind ViewModel.ColorList, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedColor4, Mode=TwoWay}"></ComboBox>
        </StackPanel>
        <Button Content="Set Move" Command="{x:Bind ViewModel.SetMoveCommand, Mode=OneWay}" Grid.Column="1" Margin="4"></Button>


    </Grid>
</UserControl>
